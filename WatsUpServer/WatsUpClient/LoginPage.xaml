<UserControl x:Class="WatsUpClient.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WatsUpClient"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30">Login/Registrazione</TextBlock>
        <TextBox Grid.Row="1" Grid.ColumnSpan="2" Name="tb_username" VerticalAlignment="Center" Width="500"/>
        <Button Grid.Row="3" Name="bt_login" Click="bt_login_Click" Width="100" Height="50">Login</Button>
        <Button Grid.Row="3" Grid.Column="1" Name="bt_registration" Click="bt_registration_Click" Width="100" Height="50">Registrati</Button>
    </Grid>
</UserControl>
